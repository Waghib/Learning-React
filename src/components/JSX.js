// JSX

// JavaScript XML (JSX) is an extension to the JavaScript language syntax. It is similar to HTML, but not exactly the same. JSX is a preprocessor step that adds XML syntax to JavaScript. You can definitely use JSX with React. The JSX code is then transpiled to JavaScript using a tool such as Babel.
// write XML like code for elements and components
// JSX tags have a tag name, attributes, and children
// JSX is not a necessity to write React applications. It is just a syntax sugar that makes your React code more elegant and readable.
// JSX makes your React code simpler and elegant. It allows you to write HTML-ish code in your JavaScript code.
// JSX code is easier to understand and write.
